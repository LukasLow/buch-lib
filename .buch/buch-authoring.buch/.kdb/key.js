// Echo the generic placeholder syntax for documentation purposes.
//
// The sequence "%%key%%" is reserved by the renderer. A page that needs to
// display it literally can have a KDB value return it verbatim: replacement
// output is not scanned again. This page uses that idiom so the syntax examples
// in these docs survive rendering.
"%%key%%";
