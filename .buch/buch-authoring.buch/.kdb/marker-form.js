// The generic shape of an inline error marker, for documentation.
//
// Returned text is never rescanned, so this renders the marker form literally
// without the page itself containing an unresolvable reference.
"%%kdb error: <key>: <message>%%";
